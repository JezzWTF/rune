# Rune extensions

Place first-party Rune extension assets in this directory. The production image
copies the directory to `/usr/lib/rune/customization/extensions` so extensions
can be packaged and consumed without overlaying an upstream code-server image.
